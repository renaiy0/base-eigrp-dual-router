# base-eigrp-dual-router
A basic EIGRP lab setup built in Cisco Packet Tracer. This project demonstrates how to configure and verify EIGRP neighbor adjacency between two routers connected via a serial link, with end devices in separate LANs.
